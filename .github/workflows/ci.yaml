name: Testing ID
on:
  workflow_dispatch:
    inputs:
      myInput:
        description: An input string
        type: string

jobs:
  myJob:
    runs-on: ubuntu-latest
    if: ${{ github.event.inputs.myInput != None }}
    steps:
    - name: Print inputs
      run: |
        echo ${{ inputs.myInput }}
    
